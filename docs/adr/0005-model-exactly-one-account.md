# 只建模一个 LINUX DO 账户

IntelliDo 永久只建模一个 LINUX DO 账户，而非仅在首个版本限制为一个活跃账户。退出登录后可以替换该身份，但产品不会保留多个身份、分隔多套浏览器会话或提供账户切换；这使身份、阅读状态、进度、通知和设置在整个架构中始终含义明确。

如果登录或重新验证以不同用户名完成，IntelliDo 将执行原子化账户替换：在为新身份加载任何内容之前，清除旧账户的私密标签页、内存内容、通知和后台状态。两个账户的状态不会共存，即使在 UI 中短暂共存也不允许。
