# 按行为契约而非精确版本支持插件功能

IntelliDo 根据已理解的行为与数据契约支持 LINUX DO 插件功能，不把网站固定到某份精确的插件清单或版本组合。未知或不兼容的插件行为将在最小受影响组件中显示明确的原生错误，并提供重试和复制诊断信息操作；错误不会扩散到无关 UI，也不会以嵌入式 Web UI 渲染。该选择接受偶发的兼容性失败，以换取服务器独立演进和始终一致的原生产品体验。

插件功能只有具备已理解的结构化服务器契约时才获得原生支持。IntelliDo 不抓取网站渲染后的 DOM；没有此类契约的路由继续作为受限的应用内网站路由处理。
