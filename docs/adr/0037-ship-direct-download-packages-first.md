# 首先发布直接下载的软件包

IntelliDo 初始版本提供 Windows 安装程序及免安装 ZIP 归档、macOS DMG 和 Linux Tarball。ZIP 并非自包含的漫游配置：会话、设置、日志、缓存和插件仍位于操作系统标准的每用户目录，而不是可执行文件旁。MSIX、Homebrew、Flatpak、Snap、Linux 发行版仓库及其他软件包管理器通道均暂缓。这样能让首个版本在所有受支持平台上直接安装，同时不把多个打包生态纳入初始发布契约。

Windows 安装程序只支持每用户安装，不需要管理员权限。全用户或系统范围安装模式暂缓。
