# 每个发布通道和操作系统用户只运行一个进程

IntelliDo 对同一发布通道、同一操作系统用户实施单进程约束。若成员再次启动同一通道，新的启动器不会创建第二个应用进程或窗口，而是唤起现有主窗口，并把受支持的启动目标转交给现有进程处理。这样可避免同一账户重复建立会话、通知连接和阅读活动上报。

稳定版与 Nightly 仍受既有的跨通道互斥约束，因此二者也不能同时运行。目标转交仅处理 IntelliDo 已支持的命令行启动目标；它不会注册自定义 URL Scheme，也不会把 IntelliDo 注册为操作系统浏览器或 `https://linux.do/` 链接处理器。
