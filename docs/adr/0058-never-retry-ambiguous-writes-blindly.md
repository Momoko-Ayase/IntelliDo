# 绝不盲目重试结果不明的写入

IntelliDo 可以在感知服务器限流的前提下，使用指数退避重试幂等读取请求。它绝不盲目重试超时或结果不明的写入，例如创建或编辑内容、回应、投票或邀请。它会先与 LINUX DO 对账资源状态，只有排除重复或意外状态反转后才提供 **重试**；服务器明确限流时显示可重试时间窗口。

Like、Bookmark 和通知状态变更等可安全撤销的操作，只有在显示待处理状态且被拒后能确定性回滚时，才可乐观更新。帖子、编辑、邀请及其他重要写入在服务器确认前绝不显示为已提交。受信任 Web 服务维护其自身状态，不把推测结果投射到 IntelliDo 原生 UI。
