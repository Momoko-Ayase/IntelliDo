# 为已约定的初始范围保留 1.0.0

IntelliDo `1.0.0` 表示产品决策中记录的完整初始范围已经完成。更早、连贯且可支持的 `0.x` 里程碑可以通过稳定版应用内更新通道发布，但发布说明必须明确指出缺失功能，且不得宣称功能完整。需要手动安装的 Nightly 仍用于集成度较低的进行中工作。

每个稳定 `0.x` 里程碑对其暴露的每项功能都已满足约定的安全、隐私、无障碍、本地化、原生体验和真实阅读要求。1.0 前状态只放宽功能完整性，不放宽产品的安全或完整性边界。
