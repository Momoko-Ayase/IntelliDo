# 提供完整的本地数据重置

IntelliDo 提供 **清除所有本地数据** 操作：退出登录，清除受信任浏览器与嵌入浏览器存储、应用偏好、日志、缓存和非捆绑插件，然后返回首次启动状态。该操作要求键入确认，并预览将删除的准确数据类别和位置。它绝不删除 LINUX DO 服务器数据，也不删除成员明确下载到 IntelliDo 管理存储之外的文件。

各平台卸载程序提供默认勾选的明确选项 **移除 IntelliDo 本地数据**。ZIP、DMG 和 Tarball 分发包则记录等效的手动清理位置。两条路径都不触碰明确下载的文件。
