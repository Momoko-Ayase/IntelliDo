# 身份安全操作保留在 LINUX DO 页面

IntelliDo 原生实现普通社区偏好，但凭据和身份安全操作使用 LINUX DO 真实的已验证身份页面，包括已验证邮箱、密码、OAuth 身份、Passkey、MFA 与恢复代码、活动会话、已授权应用和删除账户。这些页面在专用的模态 JCEF 窗口中打开，共享已验证身份的 LINUX DO 会话，并限制在获准的账户与安全路由；外部导航在系统浏览器中打开。该选择把有意设置的 Web 边界从登录扩展到身份安全，使 IntelliDo 绝不部分重做必须随服务器演进的高风险流程。
