# IntelliDo 仅支持在线使用

IntelliDo 不提供离线模式、持久话题或帖子缓存、写入队列或事后活动补报。断线后已显示内容可继续留在内存中，也可存在普通的短期传输或图片缓存；但所有持久导航、创作、搜索、Connect 和阅读活动都需要实时连接 LINUX DO。

初始产品不包含嵌入式应用数据库。IntelliJ 管理的存储仅用于本地偏好、浏览器配置、受限日志、可丢弃缓存和已安装插件；这些存储都不会成为服务器内容的影子仓库。
