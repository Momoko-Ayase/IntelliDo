# 登录后恢复导航但不恢复写入

匿名成员触发需要账户的操作时，IntelliDo 先显示原生 **需要登录** 确认，而不是直接执行操作或无提示打开登录页。成员确认后进入既有 LINUX DO 登录流程；成功登录时，IntelliDo 按新能力集合重新获取资源，并返回此前的准确目标位置。

该流程只保留导航目标，不保留可执行的写入意图。Like、回复、投票、Bookmark、关注状态变化及其他服务器写入都不会在登录后自动提交或重放；成员必须在已验证身份的原生界面中重新明确触发。取消登录则返回匿名目标或 Home，且不重复提示。

匿名阶段的浏览和可见时间仍不转化为已登录账户的阅读活动。
