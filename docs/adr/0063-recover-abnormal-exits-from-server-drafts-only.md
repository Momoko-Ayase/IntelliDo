# 异常退出后只从服务器草稿恢复

IntelliDo 检测到上一次进程异常终止后，下一次交互式启动仍按既有规则进入 Home，不恢复标签页、导航位置或本地编辑状态。Home 显示可关闭的非模态提示，并为已登录成员提供 **打开服务器草稿** 操作；该操作打开既有“草稿”工具窗口，内容仍以 LINUX DO 为权威。

IntelliDo 不为异常退出创建本地草稿、编辑器快照或会话恢复文件。只有异常终止前已成功同步到 LINUX DO 的草稿可以恢复；两次服务器自动保存之间仍只存在于内存中的内容可能丢失。该提示不会暗示未同步内容能够找回，也不会改变每次启动回到 Home 的规则。
