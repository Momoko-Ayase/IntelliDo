# 以服务器能力为权威

IntelliDo 根据 LINUX DO 针对具体资源返回的能力标志决定可用操作，再额外施加永久的 Level 3 上限。它不会仅凭信任等级推断权限，因为分类策略、群组、站点设置、所有权和内容状态都可能覆盖权限；操作被拒绝后会刷新能力，并显示服务器给出的原因。

服务器维护、只读以及仅工作人员可写状态以原生通知呈现，并提前禁用受影响操作。已经可用的可读内容可以继续显示，但被阻止的写入绝不会排队等待日后提交。

当成员有权查看时，封禁、禁言及其他账户限制会显示服务器提供的准确原因和到期时间。IntelliDo 只暴露服务器仍允许的操作，绝不重新解释或绕过限制。
