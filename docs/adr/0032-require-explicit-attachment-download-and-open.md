# 附件下载与打开必须明确触发

只有成员通过原生保存对话框明确触发后，IntelliDo 才下载附件，并原生显示进度、取消操作和服务器错误。它绝不自动打开或执行已下载文件；把已保存文件交给操作系统需要另一个明确操作，内联图片和受支持媒体则保持仅查看预览。

同一拦截规则适用于由登录页、不受支持的 LINUX DO 路由、允许列表中的受信任服务页面、OAuth 模态页和帖子嵌入发起的下载。任何 JCEF 界面都不能绕过原生“保存”、进度、取消和明确打开边界。
